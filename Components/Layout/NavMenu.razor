<div class="sidebar">
    <!-- Brand -->
    <a href="/" class="brand mb-5 text-decoration-none">
        <span class="fs-4 fw-semibold">🍏 ExpenseTracker</span>
    </a>

    <!-- Navigation Links -->
    <ul class="nav flex-column mb-auto">
        <li class="nav-item">
            <NavLink class="nav-link" activeClass="active" href="">
                <i class="bi bi-house-door-fill me-2"></i> Home
            </NavLink>
        </li>
        <li>
            <NavLink class="nav-link" activeClass="active" href="categories">
                <i class="bi bi-grid-fill me-2"></i> Categories
            </NavLink>
        </li>
        <li>
            <NavLink class="nav-link" activeClass="active" href="expenses">
                <i class="bi bi-wallet2 me-2"></i> Expenses
            </NavLink>
        </li>
        <li>
            <NavLink class="nav-link" activeClass="active" href="incomes">
                <i class="bi bi-cash-coin me-2"></i> Incomes
            </NavLink>
        </li>
    </ul>

    <!-- Footer -->
    <div class="profile mt-auto">
        <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-circle me-2 fs-5"></i>
            <span>User</span>
        </a>
        <ul class="dropdown-menu dropdown-menu-dark shadow" aria-labelledby="userDropdown">
            <li><a class="dropdown-item" href="#">Settings</a></li>
            <li><a class="dropdown-item" href="#">Profile</a></li>
            <li><hr class="dropdown-divider" /></li>
            <li><a class="dropdown-item" href="#">Sign out</a></li>
        </ul>
    </div>
</div>


<style>
    .sidebar {
        position: fixed;
        top: 20px;
        left: 20px;
        bottom: 20px;
        width: 260px;
        background: rgba(20, 20, 20, 0.75); /* Darker translucent background */
        backdrop-filter: blur(20px) saturate(180%);
        -webkit-backdrop-filter: blur(20px) saturate(180%);
        border-radius: 20px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.8); /* stronger shadow */
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #ddd; /* Softer light color */
        display: flex;
        flex-direction: column;
        padding: 1.5rem;
        z-index: 1000;
    }

        .sidebar .brand {
            color: #eee; /* light gray, not pure white */
            font-size: 1.6rem;
            font-weight: 600;
            letter-spacing: -0.5px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.6);
        }

        .sidebar .nav-link {
            color: #bbb; /* medium-light gray */
            font-weight: 500;
            border-radius: 12px;
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
        }

            .sidebar .nav-link i {
                font-size: 1.2rem;
                color: #999; /* muted icon color */
                transition: color 0.3s ease;
            }

            .sidebar .nav-link:hover,
            .sidebar .nav-link.active {
                background: rgba(10, 132, 255, 0.25);
                color: #fff;
                box-shadow: 0 4px 20px rgba(10, 132, 255, 0.5);
            }

                .sidebar .nav-link:hover i,
                .sidebar .nav-link.active i {
                    color: #0a84ff;
                }

    .profile a {
        color: #ccc; /* lighter gray for user profile */
        font-weight: 600;
        transition: color 0.3s ease;
    }

        .profile a:hover {
            color: #0a84ff;
        }

</style>
